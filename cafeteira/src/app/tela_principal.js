const soma = (valor1, valor2) => {
    return valor1+valor2;
}

const soma2 = (valor1, valor2) => valor1+valor2;

v1 = 1;
v2 = 3;
valorSomado = soma2(v1, v2);

console.log(`Soma dos valores ${v1} e ${v2} = ${valorSomado}`);